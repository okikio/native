{
    "name": "root",
    "private": true,
    "license": "MIT",
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build": "ultra -r build",
        "watch": "ultra -r watch",
        "demo:build": "gulp build",
        "demo:watch": "gulp default --watch",
        "dts": "ultra -r dts",
        "test": "ultra -r test",
        "test:watch": "ultra -r test:watch",
        "pre-release": "ultra -r pre-release && pnpm run demo:build",
        "changelog": "ultra -r --concurrency 1 changelog && git push --follow-tags origin master",
        "release": "pnpm -r publish --tag latest"
    },
    "husky": {
        "hooks": {
            "commit-msg": "commitlint -E HUSKY_GIT_PARAMS --verbose"
        }
    },
    "dependencies": {
        "@okikio/native": "workspace:*"
    },
    "devDependencies": {
        "@commitlint/cli": "^11.0.0",
        "@commitlint/config-conventional": "^11.0.0",
        "browser-sync": "^2.26.13",
        "commitizen": "^4.2.3",
        "esbuild": "^0.8.34",
        "gulp": "^4.0.2",
        "gulp-esbuild": "^0.3.17",
        "gulp-plumber": "^1.2.1",
        "gulp-pug": "^4.0.1",
        "gulp-rename": "^2.0.0",
        "gulp-sass": "^4.1.0",
        "gzip-size": "^6.0.0",
        "husky": "^4.3.8",
        "merge-stream": "^2.0.0",
        "pnpm": "^5.15.2",
        "pretty-bytes": "^5.5.0",
        "standard-version": "^9.1.0",
        "ultra-runner": "^3.9.0"
    }
}