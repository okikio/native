{
    "name": "root",
    "version": "0.0.7",
    "private": true,
    "license": "MIT",
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build": "ultra --parallel -r build",
        "watch": "ultra --parallel -r watch",
        "demo:build": "pnpm --parallel -r demo:build",
        "demo:watch": "ultra --parallel -r demo:watch",
        "dts": "ultra -r --parallel --filter \"packages/*\" dts",
        "test": "ultra -r test",
        "test:watch": "ultra --parallel -r test:watch",
        "pre-release": "ultra -r pre-release",
        "release": "npm run pre-release && standard-version && pnpm -r publish --tag latest"
    },
    "husky": {
        "hooks": {
            "commit-msg": "commitlint -E HUSKY_GIT_PARAMS --verbose"
        }
    },
    "dependencies": {
        "pnpm": "^5.14.1",
        "ultra-runner": "^3.8.0"
    },
    "devDependencies": {
        "@commitlint/cli": "^11.0.0",
        "@commitlint/config-conventional": "^11.0.0",
        "commitizen": "^4.2.2",
        "husky": "^4.3.6",
        "standard-version": "^9.0.0"
    }
}
